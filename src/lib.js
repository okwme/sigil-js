import { pour, multiPour } from './core/pour'
import patPToSvgJson from './Native';

let addThis = function (a, b) {
         return a + b;
       }


export { pour, multiPour, addThis, patPToSvgJson}
